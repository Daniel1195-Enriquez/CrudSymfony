{{include('pages/inc/header.html.twig') }}

    <h1>Ver registro</h1>

    <a class="list-group-item active">
        Registro no. {{post.id}}
    </a>
    <p class="list-group-item">{{ post.nombre }}</p>
    <p class="list-group-item">{{ post.apePaterno }}</p>
    <p class="list-group-item">{{ post.apeMaterno }}</p>
    <p class="list-group-item">{{ post.edad }}</p>
    <p class="list-group-item"><a href="/post" class="btn 
    btn-primary">Regresar</a> </p>
    
  
{{include('pages/inc/footer.html.twig') }}